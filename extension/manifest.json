{
  "manifest_version": 3,
  "name": "Tsaagan Companion",
  "version": "0.1.0",
  "description": "Trusted browser-control bridge for Tsaagan — drives the active tab via chrome.debugger Input (isTrusted=true, viewport coordinates) over a localhost long-poll. No debug port, no screen-coordinate math.",
  "permissions": ["debugger", "scripting", "tabs", "alarms"],
  "host_permissions": ["http://127.0.0.1/*", "<all_urls>"],
  "background": { "service_worker": "background.js" }
}
